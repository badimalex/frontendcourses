export const rootPath = () => '/';
export const cartPath = () => '/cart';
export const coursePath = (id: string | number = ':id') => `/courses/${id}`;
export const productPath = (id: string | number = ':id') => `/products/${id}`;
